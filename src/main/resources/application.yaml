spring:
  application:
    name: taskmanager
  datasource:
    url: ${DATABASE_URL:jdbc:mysql://localhost:3306/taskmanager_db}
    username: ${DATABASE_USERNAME:root}
    password: ${DATABASE_PASSWORD:131206ic}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false

server:
  port: ${PORT:8080}